services:
  - type: web
    name: rppg-heartrate-backend
    runtime: docker
    dockerfilePath: ./Dockerfile.cloud
    dockerContext: ./backend
    plan: starter
    envVars:
      - key: PORT
        value: 8000
    healthCheckPath: /health
